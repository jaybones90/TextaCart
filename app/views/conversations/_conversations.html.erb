<div class="conversations">
  <% @conversations.each do |conversation| %>
    <%= link_to conversation_path(conversation), remote: true do %>
      <div class="conversation-tile">
        <%= conversation.customer.phone_number %>
      </div>
    <% end %>
  <% end %>
</div>
